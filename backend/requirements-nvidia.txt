# NVIDIA Agent IQ Dependencies
# Install with: pip install -r requirements-nvidia.txt

# NVIDIA Triton Client for GPU inference
tritonclient[all]==2.40.0

# NVIDIA TensorRT for optimized inference
nvidia-tensorrt==8.6.1

# CUDA runtime libraries
nvidia-cuda-runtime-cu11==11.8.89

# PyTorch with CUDA support (for model loading)
torch==2.1.0+cu118
torchvision==0.16.0+cu118
--extra-index-url https://download.pytorch.org/whl/cu118

# ONNX Runtime with GPU support (alternative to TensorRT)
onnxruntime-gpu==1.16.3

# Utilities
numpy>=1.24.0
pyyaml>=6.0
requests>=2.31.0
